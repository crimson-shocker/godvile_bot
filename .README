vnc pass vncpassword

1 install
cd /opt/
git clone -b docker https://github.com/crimson-shocker/godvile_bot

2 first install
./startbot.sh -install

3 config  passwd

4 start bot
    ./startbot.sh 

5 add cron
0 */1 * * *	root	/opt/godvile_bot/startbot.sh >> /var/log/bot.log

Update 
./startbot.sh -update

Remove (Delete all docker images needed for the bot)
 ./startbot.sh -update

