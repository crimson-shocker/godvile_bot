vnc pass vncpassword

1 install
cd /opt/
git clone https://github.com/crimson-shocker/godvile_bot

2 build images 
    docker build -f Dockerfile -t bot .

3 config  passwd

4 start bot
    ./startbot.sh 

5 add cron

* */1 * * *	root	/opt/godvile_bot/startbot.sh >> /var/log/bot.log